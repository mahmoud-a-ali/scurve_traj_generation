#!/usr/bin/env python
from scipy import linalg
from matplotlib import pyplot as plt
import numpy as np
from numpy.polynomial import Polynomial
from sympy import Matrix, Piecewise, Symbol

import traj

# Test path
path = np.array([(0.0, 0.0), (1.0, 1.0), (1.5, 0.8), (-0.2, 0.4)])

boundaries, functions = traj.parameterize_path(path)

s = Symbol('s')
traj.plot.plot_2d_path(boundaries, functions, s, 100)
plt.plot([q[0] for q in path], [q[1] for q in path], 'bx')
plt.show()

